#forgot-password
    .close=h link_to_close_redbox "X", :controller => "realestate"

    %h3 Forgot Password
    - if flash[:notice]
        = flash[:notice]

    - form_tag url_for(:action => 'create') do
        What is the email address used to create your account?
        %br/
        = text_field_tag :email, "", :size => 50
        %br/
        %br/
        = submit_tag "Reset Password", :id => 'button'